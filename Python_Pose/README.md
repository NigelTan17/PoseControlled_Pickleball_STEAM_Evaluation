# Python Pose Module (MediaPipe + UDP)

This folder contains the webcam-based pose tracking script used to control the Unity game.

## What it does
- Captures full-body pose landmarks using MediaPipe
- Converts pose landmarks into control signals
- Sends control messages to Unity using UDP

## Requirements
Install dependencies:
```bash
pip install -r requirements.txt
